server:
  port: ${SERVER_PORT:0}
spring:
  profiles:
    active: ${PROFILE:native}
  application:
    name: eureka-server

  cloud:
    config:
      profile: native
      uri: ${CONFIG_SERVER_URL:http://localhost:8888}